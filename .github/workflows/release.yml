# Generated by tinfoil 0.2.6. Do not edit by hand.
# Regenerate with: mix tinfoil.generate
name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  MIX_ENV: prod
  APP_NAME: tinfoil_demo

jobs:
  build:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: darwin_arm64
            burrito_name: darwin_arm64
            runner: macos-latest
            triple: aarch64-apple-darwin
            burrito_os: darwin
            burrito_cpu: aarch64
            os_family: darwin
          - target: darwin_x86_64
            burrito_name: darwin_x86_64
            runner: macos-15-intel
            triple: x86_64-apple-darwin
            burrito_os: darwin
            burrito_cpu: x86_64
            os_family: darwin
          - target: linux_x86_64
            burrito_name: linux_x86_64
            runner: ubuntu-latest
            triple: x86_64-unknown-linux-musl
            burrito_os: linux
            burrito_cpu: x86_64
            os_family: linux
          - target: linux_arm64
            burrito_name: linux_arm64
            runner: ubuntu-24.04-arm
            triple: aarch64-unknown-linux-musl
            burrito_os: linux
            burrito_cpu: aarch64
            os_family: linux
          - target: windows_x86_64
            burrito_name: windows_x86_64
            runner: ubuntu-latest
            triple: x86_64-pc-windows-msvc
            burrito_os: windows
            burrito_cpu: x86_64
            os_family: windows
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Elixir/OTP
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Set up Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: "0.15.2"

      - name: Cache mix deps and build
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-${{ matrix.target }}-mix-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.target }}-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Build and package release
        run: mix tinfoil.build --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.triple }}
          path: _tinfoil/*
          if-no-files-found: error

  release:
    name: Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Elixir/OTP
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.19"
          otp-version: "28"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Cache mix deps and build
        uses: actions/cache@v5
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-release-mix-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-release-mix-

      - name: Install dependencies
        run: mix deps.get

      - name: Publish GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mix tinfoil.publish --input-dir artifacts
